name: Release
on:
  push:
    tags:
      - 'v*'
permissions:
  contents: write    # create GitHub releases
  packages: write    # publish npm + Docker to ghcr.io
  id-token: write
jobs:
  release:
    uses: fjacquet/ci/.github/workflows/web-release.yml@v1
    with:
      node-version: "24"
